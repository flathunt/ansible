pipeline {
    agent any

    options {
        ansiColor('xterm')
    }

    stages {
        stage('Clone repo') {
            steps {
                git branch: 'main', credentialsId: 'marcusc', url: 'git@github.com:flathunt/ansible.git'
            }
        }    
        stage('Run nmap') {
            steps {
	        sh '''
		    sudo python3 -m venv venv
                    . ./venv/bin/activate
		    sudo pip install python-nmap tabulate
                    sudo python3 nscan.py
                '''
            }
        }
    }
}
